// Statement and Expression
//    - Write a script containing at least two statements and two expressions. Log the results and identify which parts are statements and which are expressions.
//Statements 
const a = 20;
const b = 10 

//Expressions
let sum = a+b;
let div = a/b;

//getting the output
console.log(`the sum of a, b is : ${sum}`);
console.log(`the division of a, b is : ${div}`);